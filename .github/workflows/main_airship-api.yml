# Docs for the Azure Web Apps Deploy action: https://github.com/Azure/webapps-deploy
# More GitHub Actions for Azure: https://github.com/Azure/actions

name: Build and deploy container app to Azure Web App - airship-api

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: 'ubuntu-latest'

    steps:
    - uses: actions/checkout@v2

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v2

    - name: Log in to registry
      uses: docker/login-action@v2
      with:
        registry: https://airship.azurecr.io/
        username: ${{ secrets.AzureAppService_ContainerUsername_f6eaa3568e9c43da8a70f75ea3113594 }}
        password: ${{ secrets.AzureAppService_ContainerPassword_1a6f5f0d28434c10b66b7355c5845fb2 }}

    - name: Build and push container image to registry
      uses: docker/build-push-action@v3
      with:
        push: true
        tags: airship.azurecr.io/${{ secrets.AzureAppService_ContainerUsername_f6eaa3568e9c43da8a70f75ea3113594 }}/airship/airship-backend:${{ github.sha }}
        file: ./Dockerfile

  deploy:
    runs-on: ubuntu-latest
    needs: build
    environment:
      name: 'production'
      url: ${{ steps.deploy-to-webapp.outputs.webapp-url }}

    steps:
    - name: Deploy to Azure Web App
      id: deploy-to-webapp
      uses: azure/webapps-deploy@v2
      with:
        app-name: 'airship-api'
        slot-name: 'production'
        publish-profile: ${{ secrets.AzureAppService_PublishProfile_aa36bc4b09434e5482c874987b994cd7 }}
        images: 'airship.azurecr.io/${{ secrets.AzureAppService_ContainerUsername_f6eaa3568e9c43da8a70f75ea3113594 }}/airship/airship-backend:${{ github.sha }}'